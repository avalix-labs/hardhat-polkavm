{
  "name": "hardhat-polkadot-monorepo",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Hardhat 3 plugin for Polkadot (compile Solidity to PolkaVM via resolc).",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter hardhat-polkadot build",
    "dev": "bun run --filter hardhat-polkadot dev",
    "test": "bun run --filter hardhat-polkadot test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}

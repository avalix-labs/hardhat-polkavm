/** Default revive/resolc release used when downloading the native binary. */
export const DEFAULT_RESOLC_VERSION = '1.2.0'

/** Where the native resolc binaries are published. */
export const REVIVE_RELEASES_BASE_URL =
  'https://github.com/paritytech/revive/releases/download'

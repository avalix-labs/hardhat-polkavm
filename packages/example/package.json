{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "deploy": "hardhat run scripts/deploy.ts",
    "test": "vitest run",
    "test:testnet": "vitest run testnet",
    "test:local": "vitest run local",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-keystore": "^3.0.12",
    "@types/node": "^22.15.3",
    "hardhat": "^3.4.0",
    "hardhat-polkadot": "workspace:*",
    "typescript": "^5.8.3",
    "viem": "^2.21.0",
    "vitest": "^4.1.5"
  }
}
